const React = require('react');

const AppSpecs = React.createClass({
  render: function () {
    return (
      <div>
        App Specs component
      </div>
    );
  },
});

module.exports = AppSpecs;
