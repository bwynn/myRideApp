const React = require('react');

const Bike = React.createClass({
  render: function () {
    return (
      <div>
        Bike Component Rendered
      </div>
    );
  },
});

module.exports = Bike;
